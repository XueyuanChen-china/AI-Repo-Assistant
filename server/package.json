{
  "name": "@ai-repo-assistant/server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@ai-repo-assistant/shared": "workspace:*",
    "@fastify/cors": "^10.0.1",
    "fastify": "^5.3.2"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
